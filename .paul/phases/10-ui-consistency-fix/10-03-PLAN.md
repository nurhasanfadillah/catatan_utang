---
phase: 10-ui-consistency-fix
plan: 03
type: execute
wave: 1
depends_on: ["10-01", "10-02"]
files_modified:
  - components/TransactionForm.tsx
  - components/ConfirmationModal.tsx
  - components/ExportModal.tsx
autonomous: false
---

<objective>
## Goal
Fix semua inkonsistensi UI di TransactionForm.tsx, ConfirmationModal.tsx, dan ExportModal.tsx — standardisasi label, input border/bg, form footer, type buttons, dan cancel buttons sesuai design system.

## Purpose
Form dan modal adalah titik interaksi utama user (tambah/edit data, konfirmasi, ekspor). Inkonsistensi label slate-300 dan input border-slate-600 terasa saat user fokus ke elemen form.

## Output
- `components/TransactionForm.tsx` — 7 fix (TF1-TF7): type buttons, labels, inputs, footer
- `components/ConfirmationModal.tsx` — 3 fix (CM1-CM3): cancel button; CM4 didokumentasikan
- `components/ExportModal.tsx` — 5 fix (EM1-EM5): labels, inputs, cancel button
</objective>

<context>
@.paul/phases/09-ui-consistency-audit/AUDIT.md
@components/TransactionForm.tsx
@components/ConfirmationModal.tsx
@components/ExportModal.tsx
</context>

<acceptance_criteria>

## AC-1: TransactionForm Konsisten dengan Design System
```gherkin
Given TransactionForm masih menggunakan light mode classes di type buttons, text-slate-300 di labels, border-slate-600 dan bg-slate-700/50 di inputs, bg-slate-900/50 di footer
When semua inkonsistensi diperbaiki (TF1-TF7)
Then INCOME active button: bg-emerald-900/20 text-emerald-300 (tanpa bg-emerald-50 text-emerald-700)
  Dan EXPENSE active button: bg-rose-900/20 text-rose-300 (tanpa bg-rose-50 text-rose-700)
  Dan semua 4 label: text-slate-400 (bukan text-slate-300)
  Dan semua 3 input: border-slate-700 bg-slate-700 solid (bukan slate-600 atau /50)
  Dan form footer: bg-slate-900 solid (bukan bg-slate-900/50)
  Dan cancel button: border-slate-700 text-slate-400
```

## AC-2: ConfirmationModal Cancel Button Konsisten
```gherkin
Given ConfirmationModal cancel button masih menggunakan border-slate-600, text-slate-300, focus:ring-slate-600
When fix diterapkan (CM1-CM3)
Then cancel button: border-slate-700 text-slate-400 focus:ring-slate-700
  Dan warning variant amber didokumentasikan sebagai extended token (tidak diubah)
```

## AC-3: ExportModal Konsisten dengan Design System
```gherkin
Given ExportModal masih menggunakan text-slate-300 di labels, border-slate-600 dan bg-slate-700/50 di inputs dan cancel button
When semua inkonsistensi diperbaiki (EM1-EM5)
Then semua 2 label: text-slate-400
  Dan title input: border-slate-700 bg-slate-700 solid
  Dan 2 date inputs: border-slate-700 bg-slate-700 solid
  Dan cancel button: border-slate-700 text-slate-400
```

## AC-4: Tidak Ada Regresi Fungsional
```gherkin
Given form submit, type selection toggle, dan modal confirm/cancel masih harus berfungsi
When class diganti
Then form masih bisa disubmit (handleSubmit, handleConfirmSubmit)
  Dan INCOME/EXPENSE toggle masih berfungsi visual (active state tetap highlight)
  Dan ConfirmationModal confirm button (gradient indigo) tidak berubah
  Dan ExportModal export button (gradient indigo) tidak berubah
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix TransactionForm.tsx — TF1, TF2, TF3, TF4, TF5, TF6, TF7</name>
  <files>components/TransactionForm.tsx</files>
  <action>
    Baca TransactionForm.tsx, terapkan 7 fix berikut:

    **TF1 — L89 INCOME active button (hapus light mode classes):**
    `'bg-emerald-50 text-emerald-700 border-2 border-emerald-500 dark:bg-emerald-900/20 dark:text-emerald-300'`
    → `'bg-emerald-900/20 text-emerald-300 border-2 border-emerald-500'`
    (hapus bg-emerald-50, text-emerald-700, dan dark: prefix yang tidak diperlukan)

    **TF2 — L99-100 EXPENSE active button (hapus light mode classes):**
    `'bg-rose-50 text-rose-700 border-2 border-rose-500 dark:bg-rose-900/20 dark:text-rose-300'`
    → `'bg-rose-900/20 text-rose-300 border-2 border-rose-500'`

    **TF3 — semua 4 label (L71, L82, L110, L122):**
    `text-slate-300` → `text-slate-400`
    (4 label: "Tanggal Transaksi", "Jenis Transaksi", "Keterangan", "Nominal (Rp)")

    **TF4 + TF5 — semua 3 input (L77, L117, L132):**
    `border border-slate-600` → `border border-slate-700`
    `bg-slate-700/50` → `bg-slate-700`
    (date input L77, text input L117, number input L132 — semuanya punya dua pattern ini)

    **TF6 — L139 form footer:**
    `bg-slate-900/50` → `bg-slate-900`

    **TF7 — L143 cancel button:**
    `border border-slate-600 text-slate-300` → `border border-slate-700 text-slate-400`

    JANGAN ubah:
    - Inactive button state: `bg-slate-700/30 text-slate-400 border-2 border-transparent hover:bg-slate-700` (sudah benar)
    - Submit button gradient `from-indigo-500 to-violet-600` (sudah benar)
    - Semua logic form (handleSubmit, setDescription, setAmount, dll)
    - `ConfirmationModal` import dan usage
  </action>
  <verify>
    `grep -n "bg-emerald-50\|text-emerald-7\|bg-rose-50\|text-rose-7\|text-slate-300\|border-slate-600\|bg-slate-700/50\|bg-slate-900/50" components/TransactionForm.tsx`
    → Hasil harus kosong (0 match)
  </verify>
  <done>AC-1 satisfied: TransactionForm bersih dari light mode classes dan inkonsistensi warna</done>
</task>

<task type="auto">
  <name>Task 2: Fix ConfirmationModal.tsx — CM1, CM2, CM3 (CM4 dokumentasi)</name>
  <files>components/ConfirmationModal.tsx</files>
  <action>
    Baca ConfirmationModal.tsx, terapkan 3 fix berikut:

    **CM1 + CM2 + CM3 — L51 cancel button:**
    `border border-slate-600 text-slate-300 font-medium hover:bg-slate-700 transition-colors focus:ring-2 focus:ring-slate-600`
    → `border border-slate-700 text-slate-400 font-medium hover:bg-slate-700 transition-colors focus:ring-2 focus:ring-slate-700`
    (3 perubahan sekaligus: border-slate-600→707, text-slate-300→400, focus:ring-slate-600→707)

    **CM4 — L37 warning variant amber: TIDAK DIUBAH**
    `bg-amber-900/30 text-amber-400` dibiarkan — amber adalah extended token yang acceptable untuk warning
    semantic (berbeda dari rose yang untuk danger). Ini intentional design decision.

    JANGAN ubah:
    - Confirm button (gradient indigo atau rose-600 untuk danger)
    - backdrop, panel styling (sudah benar: slate-950/80, slate-800, slate-700, rounded-2xl)
    - Logic prop variant dan rendering
  </action>
  <verify>
    `grep -n "border-slate-600\|text-slate-300\|focus:ring-slate-600" components/ConfirmationModal.tsx`
    → Hasil harus kosong (0 match)
  </verify>
  <done>AC-2 satisfied: ConfirmationModal cancel button konsisten, CM4 didokumentasikan</done>
</task>

<task type="auto">
  <name>Task 3: Fix ExportModal.tsx — EM1, EM2, EM3, EM4, EM5</name>
  <files>components/ExportModal.tsx</files>
  <action>
    Baca ExportModal.tsx, terapkan 5 fix berikut:

    **EM1 — L38, L50 semua label:**
    `text-slate-300` → `text-slate-400`
    (2 label: "Judul Laporan" L38, "Periode Laporan" L50)

    **EM2 + EM3 — L45 title input:**
    `border border-slate-600` → `border border-slate-700`
    `bg-slate-700/50` → `bg-slate-700`

    **EM4 — L58, L67 date inputs (keduanya sama):**
    `border border-slate-600` → `border border-slate-700`
    `bg-slate-700/50` → `bg-slate-700`
    (apply ke KEDUA date input: startDate L58 dan endDate L67)

    **EM5 — L78 cancel button:**
    `border border-slate-600 text-slate-300` → `border border-slate-700 text-slate-400`

    JANGAN ubah:
    - Export button gradient `from-indigo-500 to-violet-600` (sudah benar)
    - backdrop, panel, header styling (sudah benar)
    - Logic onExport, handleSubmit
    - `text-slate-400` di Calendar icon dan helper text (sudah benar)
  </action>
  <verify>
    `grep -n "text-slate-300\|border-slate-600\|bg-slate-700/50" components/ExportModal.tsx`
    → Hasil harus kosong (0 match)
  </verify>
  <done>AC-3 satisfied: ExportModal labels, inputs, dan cancel button konsisten</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TransactionForm, ConfirmationModal, dan ExportModal sudah di-fix:
    - Type buttons: hanya dark variant (emerald/rose -900/20 dan -300)
    - Semua label: slate-400 (lebih terang dari sebelumnya)
    - Semua inputs: border-slate-700, bg-slate-700 solid
    - Form footer: bg-slate-900 solid
    - Cancel buttons: border-slate-700 text-slate-400
    - Warning variant amber: dibiarkan (intentional)
  </what-built>
  <how-to-verify>
    1. Jalankan: `npm run dev`
    2. Buka browser: http://localhost:5173
    3. Login: klik "Akses Masuk Paksa (Dev Mode)"
    4. Klik "Tambah Data" (CTA button header):
       - Form modal muncul
       - Labels ("Tanggal Transaksi", "Jenis Transaksi", dll): warna sedikit lebih terang (slate-400)
       - Klik tombol "Tagihan (Masuk)": background emerald gelap (900/20), bukan hijau muda
       - Klik tombol "Kasbon (Keluar)": background rose gelap (900/20), bukan merah muda
       - Input fields: border subtle slate-700, background slate-700 solid
       - Footer form: background slate-900 solid (tidak ada transparency)
       - Tombol "Batal": text lebih terang (slate-400)
    5. Isi form dan submit → ConfirmationModal muncul:
       - Tombol "Batal" di modal: text-slate-400, border-slate-700
       - Tombol "Ya, Simpan": gradient indigo tetap (tidak berubah)
    6. Tutup form. Klik icon "Ekspor" di header (tab Data):
       - ExportModal muncul
       - Labels: text-slate-400
       - Input fields: border-slate-700, bg-slate-700 solid
       - Tombol "Batal": text-slate-400
  </how-to-verify>
  <resume-signal>Ketik "approved" jika visual sudah benar, atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Semua file selain `components/TransactionForm.tsx`, `components/ConfirmationModal.tsx`, `components/ExportModal.tsx`
- Logic form (handleSubmit, handleConfirmSubmit, state management)
- Submit/confirm button styling (gradient indigo — sudah benar)
- Danger variant di ConfirmationModal (`bg-rose-600 hover:bg-rose-700`)
- backdrop dan panel styling (sudah benar: slate-950/80, slate-800, rounded-2xl)
- Inactive type button state di TransactionForm (sudah benar)

## SCOPE LIMITS
- Hanya fix class yang tercantum di AUDIT.md (TF1-TF7, CM1-CM3, EM1-EM5)
- CM4 (amber warning) didokumentasikan tapi TIDAK diubah
- Tidak ada perubahan TypeScript/logic
- Tidak ada penambahan fitur baru

</boundaries>

<verification>
- [ ] `grep -n "bg-emerald-50\|text-emerald-7\|bg-rose-50\|text-rose-7\|text-slate-300\|border-slate-600\|bg-slate-700/50\|bg-slate-900/50" components/TransactionForm.tsx` → 0 hasil
- [ ] `grep -n "border-slate-600\|text-slate-300\|focus:ring-slate-600" components/ConfirmationModal.tsx` → 0 hasil
- [ ] `grep -n "text-slate-300\|border-slate-600\|bg-slate-700/50" components/ExportModal.tsx` → 0 hasil
- [ ] Form type buttons toggle masih berfungsi visual
- [ ] ConfirmationModal confirm button tetap gradient indigo
</verification>

<success_criteria>
- Semua 7 fix TF1-TF7 diterapkan di TransactionForm.tsx
- Semua 3 fix CM1-CM3 diterapkan di ConfirmationModal.tsx (CM4 didokumentasikan)
- Semua 5 fix EM1-EM5 diterapkan di ExportModal.tsx
- 0 pattern lama tersisa dari grep
- Visual verified via dev mode checkpoint
- Tidak ada regresi fungsional
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/10-ui-consistency-fix/10-03-SUMMARY.md`
</output>
