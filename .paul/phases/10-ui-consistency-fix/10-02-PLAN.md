---
phase: 10-ui-consistency-fix
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - components/StatsCard.tsx
  - components/TransactionList.tsx
autonomous: false
---

<objective>
## Goal
Fix semua inkonsistensi UI di StatsCard.tsx dan TransactionList.tsx — hapus sisa light mode classes, fix gradient, standardisasi border-radius ke `rounded-2xl`.

## Purpose
StatsCard muncul di dashboard sebagai widget utama (balance, income, expense). TransactionList muncul di tab Data sebagai tabel/kartu transaksi. Keduanya punya visual mismatch yang langsung terlihat pengguna.

## Output
- `components/StatsCard.tsx` — gradient fix, bg solid slate-800, text-white, rounded-2xl
- `components/TransactionList.tsx` — header color emerald/rose -500, semua rounded-xl → rounded-2xl
</objective>

<context>
@.paul/phases/09-ui-consistency-audit/AUDIT.md
@components/StatsCard.tsx
@components/TransactionList.tsx
</context>

<acceptance_criteria>

## AC-1: StatsCard Konsisten dengan Design System
```gherkin
Given StatsCard masih menggunakan bg-white, border-slate-200, text-slate-900, rounded-xl, dan gradient via-purple-600
When semua inkonsistensi diperbaiki (SC1-SC4)
Then income/expense wrapper menjadi bg-slate-800 border-slate-700 (solid, tanpa light classes)
  Dan valueColor menjadi text-white (tanpa dark: prefix)
  Dan outer wrapper menjadi rounded-2xl
  Dan balance gradient menjadi from-indigo-500 to-violet-600 (hapus via-purple-600)
```

## AC-2: TransactionList Header Color dan Radius Konsisten
```gherkin
Given TransactionList masih menggunakan text-emerald-600, text-rose-600, dan rounded-xl
When semua inkonsistensi diperbaiki (TL1-TL5)
Then table header "Tagihan (Masuk)" menjadi text-emerald-500
  Dan table header "Kasbon (Keluar)" menjadi text-rose-500
  Dan empty state container menjadi rounded-2xl
  Dan mobile card item menjadi rounded-2xl
  Dan desktop table wrapper menjadi rounded-2xl
```

## AC-3: Tidak Ada Regresi Fungsional
```gherkin
Given StatsCard dan TransactionList adalah komponen display-only
When class diganti
Then balance card masih tampil dengan gradient indigo→violet
  Dan income/expense cards masih tampil dengan border-l accent (emerald/rose)
  Dan mobile card view dan desktop table view masih render
  Dan tombol edit/hapus di TransactionList masih muncul untuk admin
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix StatsCard.tsx — SC1, SC2, SC3, SC4</name>
  <files>components/StatsCard.tsx</files>
  <action>
    Baca StatsCard.tsx, terapkan 4 fix berikut:

    **SC4 — L23 balance gradient (hapus via-purple-600):**
    `from-indigo-500 via-purple-600 to-violet-700` → `from-indigo-500 to-violet-600`
    (shadow-lg shadow-indigo-500/25 tetap)

    **SC1 — L31 income wrapper (hapus light classes):**
    `bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 border-l-4 border-l-emerald-500`
    → `bg-slate-800 border border-slate-700 border-l-4 border-l-emerald-500`

    **SC2 — L33 income valueColor:**
    `text-slate-900 dark:text-white` → `text-white`

    **SC1 — L39 expense wrapper (hapus light classes):**
    `bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 border-l-4 border-l-rose-500`
    → `bg-slate-800 border border-slate-700 border-l-4 border-l-rose-500`

    **SC2 — L41 expense valueColor:**
    `text-slate-900 dark:text-white` → `text-white`

    **SC3 — L51 outer wrapper:**
    `rounded-xl` → `rounded-2xl`

    JANGAN ubah:
    - `iconBg`, `iconColor` di income/expense (sudah benar: emerald-500/10, rose-500/10)
    - `titleColor: 'text-slate-500 dark:text-slate-400'` — biarkan dulu (bukan scope AUDIT.md)
    - `iconBg: 'bg-white/20'` di balance (intentional — overlay di atas gradient)
    - `border-l-4 border-l-emerald-500` dan `border-l-4 border-l-rose-500` (tetap dipertahankan)
  </action>
  <verify>
    Grep untuk pattern yang harus sudah hilang:
    `grep -n "bg-white\|slate-200\|text-slate-9\|rounded-xl\|via-purple-6\|to-violet-7" components/StatsCard.tsx`
    → Hasil harus kosong (0 match)
  </verify>
  <done>AC-1 satisfied: StatsCard bersih dari light mode classes, gradient dan radius sudah benar</done>
</task>

<task type="auto">
  <name>Task 2: Fix TransactionList.tsx — TL1, TL2, TL3, TL4, TL5</name>
  <files>components/TransactionList.tsx</files>
  <action>
    Baca TransactionList.tsx, terapkan 5 fix berikut:

    **TL3 — L18 empty state container:**
    `rounded-xl` → `rounded-2xl`

    **TL4 — L31 mobile card item:**
    `rounded-xl` → `rounded-2xl`

    **TL5 — L79 desktop table wrapper:**
    `rounded-xl` → `rounded-2xl`

    **TL1 — L86 table header income:**
    `text-emerald-600` → `text-emerald-500`

    **TL2 — L87 table header expense:**
    `text-rose-600` → `text-rose-500`

    JANGAN ubah:
    - `rounded-full` pada icon badge (L34) — beda elemen
    - `text-emerald-400`, `text-rose-400` pada icon badge (L36-37) — sudah benar
    - Logic `canEdit`, tombol edit/hapus, pagination
    - Semua class selain 5 yang disebutkan di atas
  </action>
  <verify>
    Grep untuk pattern yang harus sudah hilang:
    `grep -n "rounded-xl\|text-emerald-6\|text-rose-6" components/TransactionList.tsx`
    → Hasil harus kosong atau hanya false positive di luar 3 elemen yang dimaksud
  </verify>
  <done>AC-2 satisfied: TransactionList header color dan border-radius sudah konsisten</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    StatsCard.tsx dan TransactionList.tsx sudah di-fix:
    - Balance card: gradient indigo→violet (tanpa via-purple)
    - Income/expense cards: bg-slate-800 solid (tanpa bg-white), border-slate-700, text-white
    - Semua cards/wrappers: rounded-2xl
    - Table header: emerald-500 dan rose-500
  </what-built>
  <how-to-verify>
    1. Jalankan: `npm run dev`
    2. Buka browser: http://localhost:5173
    3. Login dengan backdoor: klik "Akses Masuk Paksa (Dev Mode)"
    4. Cek Dashboard (tab Dashboard):
       - Balance card: gradient indigo ke violet, smooth tanpa purple di tengah
       - Income card (Tagihan Masuk): background slate gelap (bukan putih), border-l emerald tetap ada
       - Expense card (Kasbon Keluar): background slate gelap (bukan putih), border-l rose tetap ada
       - Semua 3 card: corner radius membulat lebih dari sebelumnya (rounded-2xl)
    5. Klik tab Data:
       - Tabel desktop: header "Tagihan (Masuk)" berwarna emerald yang lebih terang (500, bukan 600)
       - Tabel desktop: header "Kasbon (Keluar)" berwarna rose yang lebih terang (500, bukan 600)
       - Corner table wrapper: sedikit lebih membulat (rounded-2xl)
  </how-to-verify>
  <resume-signal>Ketik "approved" jika visual sudah benar, atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Semua file selain `components/StatsCard.tsx` dan `components/TransactionList.tsx`
- Logic `canEdit`, tombol edit/hapus, onEdit/onDelete callbacks
- `border-l-4 border-l-emerald-500` dan `border-l-4 border-l-rose-500` di StatsCard (accent sidebar intentional)
- `bg-white/20` di balance variant iconBg (overlay di atas gradient — intentional)

## SCOPE LIMITS
- Hanya fix class yang tercantum di AUDIT.md (SC1-SC4, TL1-TL5)
- Tidak ada perubahan TypeScript/logic
- Tidak ada penambahan fitur atau refactor
- `titleColor: 'text-slate-500 dark:text-slate-400'` tidak disentuh (bukan scope AUDIT.md)

</boundaries>

<verification>
- [ ] `grep -n "bg-white\|slate-200\|text-slate-9\|rounded-xl\|via-purple-6" components/StatsCard.tsx` → 0 hasil
- [ ] `grep -n "rounded-xl\|text-emerald-6\|text-rose-6" components/TransactionList.tsx` → 0 hasil
- [ ] Balance card gradient smooth (no purple mid-stop)
- [ ] Income/expense cards background gelap (slate-800)
- [ ] Semua card corners lebih membulat (rounded-2xl)
- [ ] Table header colors sedikit lebih terang (500 vs 600)
</verification>

<success_criteria>
- Semua 5 fix di StatsCard.tsx diterapkan (SC1×2, SC2×2, SC3, SC4)
- Semua 5 fix di TransactionList.tsx diterapkan (TL1, TL2, TL3, TL4, TL5)
- 0 pattern lama tersisa dari grep
- Visual verified via dev mode checkpoint
- Tidak ada regresi fungsional
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/10-ui-consistency-fix/10-02-SUMMARY.md`
</output>
