---
phase: 10-ui-consistency-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Sidebar.tsx
  - components/Header.tsx
autonomous: false
---

<objective>
## Goal
Fix semua inkonsistensi UI di Sidebar.tsx dan Header.tsx — hapus sisa `gray-*` class dan `bg-white` dari era sebelum redesign, ganti dengan nilai design system yang benar.

## Purpose
Sidebar dan Header adalah dua komponen yang paling banyak menyimpan sisa pre-redesign (21 temuan gabungan dari AUDIT.md Phase 09). Keduanya tampil di setiap halaman, sehingga fix di sini berdampak visual paling luas.

## Output
- `components/Sidebar.tsx` — bersih dari `gray-*` dan `bg-white`, pure slate palette
- `components/Header.tsx` — bersih dari `gray-*` dan `bg-white`, text color konsisten
</objective>

<context>
@.paul/phases/09-ui-consistency-audit/AUDIT.md
@components/Sidebar.tsx
@components/Header.tsx
</context>

<acceptance_criteria>

## AC-1: Sidebar Bersih dari gray-* dan bg-white
```gherkin
Given Sidebar.tsx saat ini menggunakan gray-* dan bg-white (15 temuan)
When semua class diganti ke slate palette
Then tidak ada satu pun class `gray-`, `bg-white`, `slate-50`, `slate-100`, `slate-200` tersisa di Sidebar.tsx
  Dan warna border menjadi slate-700 (bukan slate-800 atau gray-*)
  Dan text color menggunakan white atau slate-100/400 sesuai hierarki
```

## AC-2: Header Bersih dari gray-* dan bg-white
```gherkin
Given Header.tsx saat ini menggunakan bg-white, gray-200, gray-800, slate-50, slate-100, slate-200, slate-300
When semua class diganti ke slate palette
Then tidak ada satu pun class `gray-`, `bg-white`, `slate-50`, `slate-100`, `slate-200` tersisa di Header.tsx
  Dan header bg menjadi bg-slate-900 (solid, tanpa transparency)
  Dan title text menjadi text-white
  Dan export button text menjadi text-slate-400
```

## AC-3: Tidak Ada Regresi Fungsional
```gherkin
Given Sidebar collapse, mobile overlay, dan nav active state masih berfungsi
When perubahan class diterapkan
Then collapse button masih toggle sidebar
  Dan active tab masih punya highlight visual (bg-indigo-500/10 text-indigo-400)
  Dan mobile sidebar overlay masih muncul di layar kecil
  Dan logout button masih berwarna rose (bukan slate)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix Sidebar.tsx — hapus semua gray-* dan bg-white</name>
  <files>components/Sidebar.tsx</files>
  <action>
    Baca Sidebar.tsx, lalu terapkan semua fix berikut secara tepat (line by line dari AUDIT.md):

    **SB1 — L48 aside bg:**
    `bg-white dark:bg-slate-900` → `bg-slate-900`

    **SB2 — L48 aside border:**
    `border-r border-gray-200 dark:border-slate-800` → `border-r border-slate-700`

    **SB3 — L57 sidebar header border:**
    `border-b border-gray-100 dark:border-slate-800` → `border-b border-slate-700`

    **SB4 — L63 logo title text:**
    `text-gray-900 dark:text-white` → `text-white`

    **SB6 — L75 collapse button:**
    `bg-white dark:bg-slate-900` → `bg-slate-900`
    `border border-gray-200 dark:border-slate-700` → `border border-slate-700`

    **SB7 — L92 nav item inactive text:**
    `text-slate-600 dark:text-slate-400` → `text-slate-400`

    **SB8 — L92 nav item inactive hover:**
    `hover:bg-slate-50 dark:hover:bg-slate-800` → `hover:bg-slate-800`

    **SB10 — L107 bottom border:**
    `border-t border-gray-100 dark:border-slate-800` → `border-t border-slate-700`

    **SB11 — L107 bottom bg:**
    `bg-white dark:bg-slate-900` → `bg-slate-900`

    **SB12 — L109 user info card:**
    `bg-gray-50 dark:bg-slate-800/50` → `bg-slate-800`
    `border border-gray-100 dark:border-slate-700` → `border border-slate-700`

    **SB13 — L120 user name text:**
    `text-gray-900 dark:text-white` → `text-white`

    **SB14 — L121 user role text:**
    `text-slate-500 dark:text-slate-400` → `text-slate-400`

    **SB15 — L128 logout text color:**
    `text-rose-600` → `text-rose-500`

    **SB16 — L128 logout hover:**
    `hover:bg-rose-50 dark:hover:bg-rose-900/20` → `hover:bg-rose-900/20`

    JANGAN ubah:
    - Logic collapse/expand sidebar
    - `dark:` prefix yang tidak ada padanan non-dark-nya
    - `bg-gradient-to-br from-indigo-500 to-violet-600` pada logo dan avatar (sudah benar)
    - `bg-indigo-500/10 text-indigo-400` pada active nav item (sudah benar)
    - `bg-black/50 backdrop-blur-sm` pada overlay mobile (sudah benar)
  </action>
  <verify>
    Grep untuk pattern yang harus sudah hilang:
    - `grep -n "gray-\|bg-white\|slate-50\b\|slate-100\b\|slate-200\b\|rose-600\|rose-50" components/Sidebar.tsx`
    → Hasil harus kosong (0 match)
  </verify>
  <done>AC-1 satisfied: Tidak ada gray-* atau bg-white di Sidebar.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Fix Header.tsx — hapus gray-* dan standardisasi text color</name>
  <files>components/Header.tsx</files>
  <action>
    Baca Header.tsx, lalu terapkan semua fix berikut:

    **H1 — L25 header bg:**
    `bg-white dark:bg-slate-900/95` → `bg-slate-900`
    (hapus transparency /95, hapus bg-white)

    **H2 — L25 header border:**
    `border-b border-gray-200 dark:border-slate-800` → `border-b border-slate-700`

    **H3 — L31 title text:**
    `text-gray-800 dark:text-white` → `text-white`

    **H4 — L44 theme toggle button:**
    Sekarang: `p-2.5 rounded-xl text-slate-500 bg-slate-50 hover:bg-slate-100 dark:bg-slate-800 dark:hover:bg-slate-700 transition-all border border-slate-200 dark:border-slate-700 hidden sm:flex`
    Fix: `p-2.5 rounded-xl text-slate-400 bg-slate-800 hover:bg-slate-700 transition-all border border-slate-700 hidden sm:flex`
    (hapus light mode classes, ganti text-slate-500 → text-slate-400)

    **H5 — L50 export button border:**
    `border border-slate-300 dark:border-slate-700` → `border border-slate-700`

    **H6 — L50 export button text:**
    `text-slate-700 dark:text-slate-300` → `text-slate-400`
    Juga: `hover:bg-slate-50 dark:hover:bg-slate-800` → `hover:bg-slate-800`

    JANGAN ubah:
    - CTA "Tambah Data" button (gradient indigo→violet sudah benar)
    - Logic `activeTab !== 'dashboard'` untuk hide/show buttons
    - Mobile menu button (sudah sederhana)
  </action>
  <verify>
    Grep untuk pattern yang harus sudah hilang:
    - `grep -n "gray-\|bg-white\|slate-50\b\|slate-100\b\|slate-200\b\|slate-300\b\|slate-700 dark:\|/95" components/Header.tsx`
    → Hasil harus kosong atau hanya false positive (bukan dari class yang diubah)
  </verify>
  <done>AC-2 satisfied: Tidak ada gray-* atau bg-white di Header.tsx, text color sudah slate-400/white</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Sidebar.tsx dan Header.tsx sudah di-fix:
    - Semua gray-* dan bg-white dihapus
    - Border slate-700 konsisten
    - Text color white/slate-400 sesuai hierarki
    - Logout rose-500
    - Header bg slate-900 solid
  </what-built>
  <how-to-verify>
    1. Jalankan: `npm run dev`
    2. Buka browser: http://localhost:5173 (atau port yang muncul)
    3. Di halaman login, cek Header: background harus `slate-900` solid (bukan putih)
    4. Login dengan backdoor: klik "Akses Masuk Paksa (Dev Mode)"
    5. Cek Sidebar:
       - Background sidebar: harus slate-900 gelap (bukan putih)
       - Border kanan sidebar: harus slate-700 subtle (bukan border terang abu-abu)
       - Nama user di footer sidebar: harus putih/slate-100
       - Role user: harus slate-400 (abu-abu medium)
       - Tombol Logout: harus rose-500 (bukan rose-600 yang lebih gelap)
       - Nav item inactive: slate-400 (bukan slate-600 yang lebih gelap)
    6. Cek Header:
       - Header background: slate-900 solid (bukan putih, bukan transparan)
       - Page title: putih
       - Icon theme toggle: slate-400 (bukan slate-500)
       - Collapse sidebar (klik tombol di tepi sidebar): pastikan masih berfungsi
       - Active nav item: masih ada highlight indigo
  </how-to-verify>
  <resume-signal>Ketik "approved" jika visual sudah benar, atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Semua file selain `components/Sidebar.tsx` dan `components/Header.tsx`
- Logic collapse, mobile overlay, active state di Sidebar
- Logic hide/show action buttons di Header berdasarkan activeTab
- Gradient `from-indigo-500 to-violet-600` pada logo, avatar, CTA button

## SCOPE LIMITS
- Hanya fix class yang tercantum di AUDIT.md (SB1-SB16, H1-H6)
- Tidak ada penambahan fitur baru
- Tidak ada perubahan TypeScript/logic
- `dark:` variant yang SUDAH BENAR tidak perlu diubah

</boundaries>

<verification>
- [ ] `grep -n "gray-\|bg-white" components/Sidebar.tsx` → 0 hasil
- [ ] `grep -n "gray-\|bg-white" components/Header.tsx` → 0 hasil
- [ ] Sidebar collapse masih berfungsi
- [ ] Active nav item masih punya highlight indigo
- [ ] Logout button masih rose
- [ ] Header title putih, background slate-900
</verification>

<success_criteria>
- Semua task selesai
- 0 gray-* atau bg-white tersisa di kedua file
- Visual verified via dev mode checkpoint
- Tidak ada regresi fungsional
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/10-ui-consistency-fix/10-01-SUMMARY.md`
</output>
