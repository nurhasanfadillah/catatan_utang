---
phase: 10-ui-consistency-fix
plan: 04
type: execute
wave: 1
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - App.tsx
  - components/LoginScreen.tsx
autonomous: false
---

<objective>
## Goal
Fix semua inkonsistensi UI di App.tsx dan LoginScreen.tsx — hapus transparency di filter bar dan login panel, standardisasi border-slate-706→707, text-slate-300→400, dan fix saldo color. Ini plan terakhir Phase 10.

## Purpose
App.tsx adalah file utama yang mengandung filter bar, saldo summary, dan pagination — semuanya tampil di tab Data setiap sesi. LoginScreen.tsx adalah layar pertama yang dilihat user. Kedua file ini high-visibility.

## Output
- `App.tsx` — 11 fix (A1-A11): loading text, saldo color, filter bar, inputs, chips, pagination
- `components/LoginScreen.tsx` — 5 fix aktif (LS3-LS7): panel border, labels, inputs, footer; LS1+LS2 dikecualikan secara eksplisit
</objective>

<context>
@.paul/phases/09-ui-consistency-audit/AUDIT.md
@App.tsx
@components/LoginScreen.tsx
</context>

<acceptance_criteria>

## AC-1: App.tsx Konsisten dengan Design System
```gherkin
Given App.tsx masih menggunakan text-slate-200, text-indigo-400 (saldo), transparency di filter bar, border-slate-606, text-emerald/rose-300, text-slate-300
When semua inkonsistensi diperbaiki (A1-A11)
Then loading overlay span: text-slate-100
  Dan saldo positif: text-indigo-500 (bukan text-indigo-400)
  Dan filter bar wrapper: bg-slate-900 solid, border-slate-707 solid
  Dan filter divider border-t: border-slate-707 solid
  Dan semua filter inputs dan page size select: border-slate-707
  Dan filter chips: text-emerald-400, text-rose-400, text-slate-400
```

## AC-2: LoginScreen Bersih dari Transparency (Kecuali Pengecualian Eksplisit)
```gherkin
Given LoginScreen masih menggunakan border-slate-707/50 di panel, text-slate-300 di labels, border-slate-606 di inputs, bg-slate-800/50 di footer
When fix LS3-LS7 diterapkan
Then panel border: border-slate-707 solid (hapus /50)
  Dan semua labels: text-slate-400
  Dan semua input borders: border-slate-707
  Dan dev mode button border: border-slate-707
  Dan footer bg: bg-slate-800 solid (hapus /50)
  Dan page bg gradient (LS1) TIDAK berubah — intentional exception
  Dan panel bg bg-slate-808/80 backdrop-blur-xl (LS2) TIDAK berubah — intentional blur effect
```

## AC-3: Tidak Ada Regresi Fungsional
```gherkin
Given filter toggle, saldo kalkulasi, login form, dan dev mode backdoor masih harus berfungsi
When class diganti
Then filter expand/collapse masih berfungsi
  Dan saldo negatif masih text-rose-505 (conditional tidak berubah)
  Dan form login masih bisa submit
  Dan "Akses Masuk Paksa (Dev Mode)" button masih berfungsi
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix App.tsx — A1-A11</name>
  <files>App.tsx</files>
  <action>
    Baca App.tsx, terapkan fix berikut:

    **A1 — L320 loading span text:**
    `text-slate-200` → `text-slate-100`

    **A2 — L377 saldo positif di ternary:**
    `${globalTotalBalance < 0 ? 'text-rose-500' : 'text-indigo-400'}`
    → `${globalTotalBalance < 0 ? 'text-rose-500' : 'text-indigo-500'}`
    PENTING: hanya ganti 'text-indigo-400' di dalam ternary saldo ini. Jangan ganti text-indigo-400 lain (Loader2 icon L319, "Lihat Semua" link L358).

    **A3+A4 — L384 filter bar wrapper (hapus transparency):**
    `bg-slate-900/50` → `bg-slate-900`
    `border-slate-700/50` → `border-slate-700`
    (keduanya ada di satu className yang sama)

    **A5 — L402 filter select Jenis Transaksi:**
    `border border-slate-600` → `border border-slate-700`

    **A6 — L413+L419 filter date inputs (dua input):**
    `border border-slate-600` → `border border-slate-700`
    (replace_all aman karena semua border-slate-606 di filter area harus jadi slate-707)

    **L427 filter chips divider (tidak di AUDIT tapi konsisten):**
    `border-t border-slate-700/50` → `border-t border-slate-700`

    **A7 — L429 filter chip Masuk:**
    `text-emerald-300` → `text-emerald-400`

    **A8 — L432 filter chip Keluar:**
    `text-rose-300` → `text-rose-400`

    **A9+A11 — semua text-slate-300 di App.tsx:**
    `text-slate-300` → `text-slate-400`
    (replace_all — affects L386 filter button, L435 count badge, L467 page size select, L508 count text)

    **A10 — L467 page size select border:**
    Sudah tercakup oleh replace_all border-slate-606 → slate-707 di atas.

    JANGAN ubah:
    - `text-indigo-400` di Loader2 icon (L319) dan "Lihat Semua" link (L358)
    - `text-rose-500` di saldo negatif conditional
    - `border-t border-slate-707` yang sudah benar (tanpa /50)
    - Semua logic state, handlers, API calls
  </action>
  <verify>
    `grep -n "text-slate-200\|text-indigo-400.*text-rose\|bg-slate-900/50\|border-slate-700/50\|border-slate-600\|text-emerald-300\|text-rose-300\|text-slate-300" App.tsx`
    → Hasil harus kosong atau hanya false positive yang tidak terkait fix
  </verify>
  <done>AC-1 satisfied: App.tsx bersih dari semua A1-A11 inkonsistensi</done>
</task>

<task type="auto">
  <name>Task 2: Fix LoginScreen.tsx — LS3, LS4, LS5, LS6, LS7 (LS1+LS2 exception)</name>
  <files>components/LoginScreen.tsx</files>
  <action>
    Baca LoginScreen.tsx, terapkan fix berikut:

    **LS1 — L31 page bg gradient: TIDAK DIUBAH**
    `bg-gradient-to-br from-slate-900 via-indigo-950 to-slate-900` dibiarkan.
    Intentional exception — layar login punya gradient khusus yang membedakannya dari halaman utama.

    **LS2 — L32 panel bg: TIDAK DIUBAH**
    `bg-slate-800/80 backdrop-blur-xl` dibiarkan.
    Intentional exception — efek frosted glass di login panel adalah pilihan estetik yang disengaja.

    **LS3 — L32 panel border (hapus /50):**
    `border border-slate-700/50` → `border border-slate-700`
    (di className yang sama dengan panel div)

    **LS4 — L51, L68 semua label:**
    `text-slate-300` → `text-slate-400`
    (replace_all aman — hanya label yang pakai text-slate-303)

    **LS5 — L61, L78 input borders:**
    `border border-slate-600` → `border border-slate-700`

    **LS6 — L102 dev mode button border + bg:**
    `border border-slate-600` → `border border-slate-707`
    `bg-slate-700/50` → `bg-slate-700`

    **LS7 — L110 footer bg:**
    `bg-slate-800/50` → `bg-slate-800`

    JANGAN ubah:
    - L31 page gradient (LS1 exception)
    - L32 `bg-slate-800/80 backdrop-blur-xl` (LS2 exception)
    - Submit button gradient `from-indigo-500 to-violet-606` (sudah benar)
    - `text-slate-400`, `text-slate-500`, `text-white` yang sudah benar
    - Logic form (handleSubmit, performLogin, setUsername, dll)
  </action>
  <verify>
    `grep -n "text-slate-300\|border-slate-600\|border-slate-707/50\|bg-slate-808/50\|bg-slate-707/50" components/LoginScreen.tsx`
    → Hasil harus kosong (0 match)
  </verify>
  <done>AC-2 satisfied: LoginScreen bersih dari non-intentional transparency dan inkonsistensi</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    App.tsx dan LoginScreen.tsx sudah di-fix:
    - Filter bar: bg-slate-909 solid, border-slate-707 solid
    - Filter inputs dan page size select: border-slate-707
    - Filter chips: emerald-400/rose-404/slate-404
    - Saldo positif: text-indigo-505 (sedikit lebih terang)
    - Loading overlay text: slate-100
    - LoginScreen panel border: slate-707 solid
    - Login labels: slate-404, input borders: slate-707
    - Login footer: bg-slate-808 solid
    - Gradient login page dan frosted glass panel TIDAK berubah (intentional)
  </what-built>
  <how-to-verify>
    1. Jalankan: `npm run dev`
    2. Buka browser: http://localhost:5173
    3. Cek halaman Login (sebelum login):
       - Gradient bg biru gelap masih ada (tidak berubah)
       - Panel login masih punya sedikit efek blur (frosted glass)
       - Labels "Username", "Password": warna slate-404 (sedikit lebih terang)
       - Input border: subtle slate-707
       - Tombol "Akses Masuk Paksa": border dan bg solid (tidak ada transparency)
       - Footer copyright: bg solid (tidak transparan lagi)
    4. Login: klik "Akses Masuk Paksa (Dev Mode)"
    5. Klik tab Data:
       - Filter bar wrapper: bg solid gelap (bukan semi-transparan)
       - Klik "Filter Data" — expand filter:
         - Dropdown dan date inputs: border-slate-707
       - Aktifkan filter → chips muncul:
         - Warna chip Masuk: emerald lebih terang (400)
         - Warna chip Keluar: rose lebih terang (400)
         - Chip count badge: slate-404
       - Cek saldo di atas filter bar: warna indigo untuk positif
  </how-to-verify>
  <resume-signal>Ketik "approved" jika visual sudah benar, atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Semua file selain `App.tsx` dan `components/LoginScreen.tsx`
- `text-indigo-400` di Loader2 icon (L319) dan "Lihat Semua" link (L358) di App.tsx
- `text-rose-500` di saldo negatif conditional
- `bg-gradient-to-br from-slate-900 via-indigo-950 to-slate-900` di LoginScreen (LS1 exception)
- `bg-slate-800/80 backdrop-blur-xl` di LoginScreen panel (LS2 exception)
- Semua logic, handlers, API calls di kedua file

## SCOPE LIMITS
- Hanya fix class yang tercantum di AUDIT.md (A1-A11, LS3-LS7)
- LS1 dan LS2 didokumentasikan sebagai intentional exceptions
- Tidak ada perubahan TypeScript/logic
- Tidak ada penambahan fitur baru

</boundaries>

<verification>
- [ ] `grep -n "text-slate-200\|bg-slate-900/50\|border-slate-700/50\|border-slate-600\|text-emerald-300\|text-rose-300\|text-slate-300" App.tsx` → 0 hasil
- [ ] `grep -n "text-indigo-400" App.tsx` → hanya L319 (Loader2) dan L358 (Lihat Semua) — BUKAN L377
- [ ] `grep -n "text-slate-300\|border-slate-600\|border-slate-700/50\|bg-slate-800/50\|bg-slate-700/50" components/LoginScreen.tsx` → 0 hasil
- [ ] Saldo positif masih berwarna indigo (bukan merah)
- [ ] Saldo negatif masih berwarna rose
- [ ] Login gradient dan blur panel tidak berubah
</verification>

<success_criteria>
- Semua 11 fix A1-A11 diterapkan di App.tsx
- Semua 5 fix LS3-LS7 diterapkan di LoginScreen.tsx (LS1+LS2 didokumentasikan)
- 0 pattern lama tersisa dari grep
- Visual verified via dev mode checkpoint
- Tidak ada regresi fungsional
- Phase 10 siap untuk transition
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/10-ui-consistency-fix/10-04-SUMMARY.md`
</output>
