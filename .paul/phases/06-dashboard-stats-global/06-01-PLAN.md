---
phase: 06-dashboard-stats-global
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
autonomous: true
---

<objective>
## Goal
Ubah card Total Tagihan dan Total Kasbon di dashboard agar menampilkan total global (semua data), bukan page-level — dan ubah layout menjadi 2-kolom compact di bawah balance card.

## Purpose
`api.getBalanceSummary()` sudah return `{ income, expense, balance }` (global), tapi `income` dan `expense` belum disimpan ke state — hanya `balance` yang dipakai. Card tagihan/kasbon saat ini pakai `processedData.summary.income/expense` yang hanya menghitung page saat ini, bukan semua data.

## Output
- `App.tsx` — 2 state baru, fetchData update, dashboard layout 2-col compact
</objective>

<context>
@.paul/STATE.md
@App.tsx
</context>

<acceptance_criteria>

## AC-1: Card Tagihan & Kasbon Menampilkan Total Global
```gherkin
Given ada 100 transaksi di database (10 halaman)
When user membuka dashboard (hanya page 1 yang di-load)
Then card "Total Tagihan" menampilkan jumlah SEMUA tagihan (bukan hanya page 1)
  Dan card "Total Kasbon" menampilkan jumlah SEMUA kasbon (bukan hanya page 1)
  Dan nilainya sama dengan angka yang direturn oleh /api/transactions/summary
```

## AC-2: Layout 2-Kolom Compact
```gherkin
Given user membuka tab Dashboard
When halaman dashboard render
Then balance card tetap di baris atas (full width / col-span)
  Dan di bawahnya: 2 card compact dalam grid 2 kolom (Tagihan | Kasbon)
  Dan card compact lebih kecil dari balance card (padding lebih kecil, font lebih kecil)
```

## AC-3: Label Tanpa "(Halaman Ini)"
```gherkin
Given card tagihan dan kasbon menampilkan data global
When user melihat label card
Then label adalah "Total Tagihan" dan "Total Kasbon" (tanpa suffix "(Halaman Ini)")
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Tambah global income/expense state + update fetchData + redesign dashboard layout</name>
  <files>App.tsx</files>
  <action>
    Lakukan 3 perubahan di App.tsx:

    **1. Tambah 2 state baru** di bawah `// Balance State` section (~baris 48-50):
    ```tsx
    // Balance State
    const [globalTotalBalance, setGlobalTotalBalance] = useState(0);
    const [globalTotalIncome, setGlobalTotalIncome] = useState(0);
    const [globalTotalExpense, setGlobalTotalExpense] = useState(0);
    const [pageStartingBalance, setPageStartingBalance] = useState(0);
    ```

    **2. Update fetchData** — tambah 2 setter setelah `setGlobalTotalBalance`:
    Cari baris:
    ```tsx
      const summary = await api.getBalanceSummary();
      setGlobalTotalBalance(summary.balance);
    ```
    Ubah menjadi:
    ```tsx
      const summary = await api.getBalanceSummary();
      setGlobalTotalBalance(summary.balance);
      setGlobalTotalIncome(summary.income);
      setGlobalTotalExpense(summary.expense);
    ```

    **3. Redesign dashboard section** — ubah dari 3-card grid menjadi balance card + 2-col compact:
    Cari block ini di dashboard section:
    ```tsx
              <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
                <StatsCard title="Saldo Saat Ini" value={globalTotalBalance} icon={Wallet} variant="balance" />
                <StatsCard title="Total Tagihan (Halaman Ini)" value={processedData.summary.income} icon={TrendingUp} variant="income" />
                <StatsCard title="Total Kasbon (Halaman Ini)" value={processedData.summary.expense} icon={TrendingDown} variant="expense" />
              </div>
    ```
    Ubah menjadi:
    ```tsx
              <StatsCard title="Saldo Saat Ini" value={globalTotalBalance} icon={Wallet} variant="balance" />
              <div className="grid grid-cols-2 gap-4">
                <div className="bg-slate-800 rounded-xl p-4 border border-slate-700 border-l-4 border-l-emerald-500">
                  <div className="flex items-center justify-between">
                    <div>
                      <p className="text-xs font-medium text-slate-400">Total Tagihan</p>
                      <p className="text-lg font-bold text-white mt-1">{formatCurrency(globalTotalIncome)}</p>
                    </div>
                    <div className="p-2 rounded-lg bg-emerald-500/10">
                      <TrendingUp className="w-4 h-4 text-emerald-500" />
                    </div>
                  </div>
                </div>
                <div className="bg-slate-800 rounded-xl p-4 border border-slate-700 border-l-4 border-l-rose-500">
                  <div className="flex items-center justify-between">
                    <div>
                      <p className="text-xs font-medium text-slate-400">Total Kasbon</p>
                      <p className="text-lg font-bold text-white mt-1">{formatCurrency(globalTotalExpense)}</p>
                    </div>
                    <div className="p-2 rounded-lg bg-rose-500/10">
                      <TrendingDown className="w-4 h-4 text-rose-500" />
                    </div>
                  </div>
                </div>
              </div>
    ```

    Tidak perlu ubah StatsCard component — compact cards dibuat inline.
    `TrendingUp` dan `TrendingDown` sudah diimport dari Phase 05.
    `formatCurrency` sudah diimport.
  </action>
  <verify>npx tsc --noEmit — tidak ada TypeScript error di App.tsx</verify>
  <done>AC-1, AC-2, AC-3 satisfied: globalTotalIncome/Expense di state, fetchData update, dashboard 2-col compact layout</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `components/StatsCard.tsx` — tidak perlu diubah
- `api/transactions/summary.ts` — sudah return global income/expense
- `services/api.ts` — getBalanceSummary sudah correct
- Logic fetchData selain 2 baris setter baru
- Tab data dan settings

## SCOPE LIMITS
- Hanya App.tsx
- Tidak mengubah StatsCard component
- Tidak mengubah API layer

</boundaries>

<verification>
- [ ] `npx tsc --noEmit` bersih di App.tsx
- [ ] `globalTotalIncome` dan `globalTotalExpense` ada sebagai useState(0)
- [ ] `setGlobalTotalIncome(summary.income)` dan `setGlobalTotalExpense(summary.expense)` ada di fetchData
- [ ] Dashboard section: balance card + `grid grid-cols-2 gap-4` dengan 2 compact cards
- [ ] Tidak ada teks "(Halaman Ini)" di dashboard cards
- [ ] `processedData.summary.income/expense` tidak lagi dipakai di dashboard
</verification>

<success_criteria>
- Task selesai, TypeScript clean
- AC-1, AC-2, AC-3 terpenuhi
- Tidak ada regresi
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/06-dashboard-stats-global/06-01-SUMMARY.md`
</output>
