---
phase: 05-kasbon-ui-improvement
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - App.tsx
autonomous: true
---

<objective>
## Goal
Deduplikasi dua blok pagination yang hampir identik di App.tsx menjadi satu blok tunggal kondisional.

## Purpose
Saat ini App.tsx punya ~80 baris kode pagination yang duplikat (baris 451-538) — satu untuk `!isFilterActive` (dengan page size selector + numbered pages) dan satu untuk `isFilterActive` (hanya prev/next). Duplikasi ini maintenance burden dan prone to drift jika ada perubahan styling pagination di masa depan.

## Output
- `App.tsx` — dua blok pagination digabung menjadi satu blok dengan kondisi inline
</objective>

<context>
## Project Context
@.paul/STATE.md
@.paul/phases/05-kasbon-ui-improvement/05-01-SUMMARY.md

## Source Files
@App.tsx
</context>

<acceptance_criteria>

## AC-1: Satu Blok Pagination
```gherkin
Given user berada di tab "Data Kasbon" dengan data ada
When filter tidak aktif (!isFilterActive)
Then pagination menampilkan: info "Menampilkan X sampai Y dari Z data", page size selector, numbered pages (di sm+), prev/next buttons

When filter aktif (isFilterActive)
Then pagination menampilkan: info "Menampilkan X sampai Y dari Z data", prev/next buttons, page indicator "X / Y" (tanpa page size selector dan tanpa numbered pages)
```

## AC-2: Tidak Ada Duplikat Pagination
```gherkin
Given developer membaca App.tsx
When melihat section pagination di dalam tab "data"
Then hanya ada SATU blok kondisional pagination (bukan dua blok terpisah)
  Dan conditional logic ada di dalam satu blok, bukan di dua blok yang diulang
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Merge dua blok pagination menjadi satu di App.tsx</name>
  <files>App.tsx</files>
  <action>
    Temukan dua blok pagination di dalam `{activeTab === 'data' && ...}` section.

    **Blok 1 (baris ~451-513):** Aktif saat `!isFilterActive && totalCount > 0`
    **Blok 2 (baris ~515-538):** Aktif saat `isFilterActive && totalCount > 0`

    **Ganti keduanya** dengan satu blok terpadu:
    ```tsx
    {totalCount > 0 && (
      <div className="mt-6 flex flex-col sm:flex-row items-center justify-between gap-4 pt-6 border-t border-slate-700">
        <div className="text-sm text-slate-400 order-2 sm:order-1">
          Menampilkan{' '}
          <span className="font-semibold text-slate-100">{((page - 1) * pageSize) + 1}</span>
          {' '}sampai{' '}
          <span className="font-semibold text-slate-100">{Math.min(page * pageSize, totalCount)}</span>
          {' '}dari{' '}
          <span className="font-semibold text-slate-100">{totalCount}</span>
          {' '}data
        </div>

        <div className="flex items-center gap-4 order-1 sm:order-2 w-full sm:w-auto justify-between sm:justify-end">
          {!isFilterActive && (
            <select
              value={pageSize}
              onChange={handlePageSizeChange}
              className="h-9 px-2 rounded-lg border border-slate-600 bg-slate-800 text-sm text-slate-300 outline-none focus:ring-2 focus:ring-indigo-500"
            >
              <option value={10}>10 / halaman</option>
              <option value={25}>25 / halaman</option>
              <option value={50}>50 / halaman</option>
              <option value={100}>100 / halaman</option>
            </select>
          )}

          <div className="flex items-center gap-1">
            <button
              onClick={() => handlePageChange(page - 1)}
              disabled={page === 1}
              className="p-2 rounded-lg border border-slate-700 text-slate-400 hover:bg-slate-700 disabled:opacity-40 disabled:cursor-not-allowed"
            >
              <ChevronLeft size={16} />
            </button>

            {!isFilterActive && (
              <div className="hidden sm:flex items-center gap-1">
                {getPageNumbers().map((p, idx) => (
                  <React.Fragment key={idx}>
                    {p === '...' ? (
                      <span className="px-2 text-slate-500">...</span>
                    ) : (
                      <button
                        onClick={() => handlePageChange(p as number)}
                        className={`min-w-[32px] h-8 px-2 rounded-lg text-sm font-medium transition-colors ${
                          page === p
                            ? 'bg-indigo-600 text-white shadow-indigo-500/30'
                            : 'text-slate-400 hover:bg-slate-700'
                        }`}
                      >
                        {p}
                      </button>
                    )}
                  </React.Fragment>
                ))}
              </div>
            )}

            <div className={`flex items-center px-2 text-sm font-medium text-slate-300 ${!isFilterActive ? 'sm:hidden' : ''}`}>
              {page} / {totalPages}
            </div>

            <button
              onClick={() => handlePageChange(page + 1)}
              disabled={page === totalPages}
              className="p-2 rounded-lg border border-slate-700 text-slate-400 hover:bg-slate-700 disabled:opacity-40 disabled:cursor-not-allowed"
            >
              <ChevronRight size={16} />
            </button>
          </div>
        </div>
      </div>
    )}
    ```

    Pastikan hapus kedua blok lama secara lengkap. Hasilnya: satu blok `{totalCount > 0 && (...)}` menggantikan dua blok sebelumnya.

    Tidak ada perubahan pada fungsi `handlePageChange`, `handlePageSizeChange`, `getPageNumbers`, atau state pagination.
  </action>
  <verify>npx tsc --noEmit — tidak ada TypeScript error; grep "isFilterActive && totalCount" App.tsx tidak menghasilkan dua blok pagination terpisah</verify>
  <done>AC-1 dan AC-2 satisfied: satu blok pagination tunggal dengan conditional logic untuk page size selector dan numbered pages</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Fungsi `handlePageChange`, `handlePageSizeChange`, `getPageNumbers` — logika tidak berubah
- State pagination: `page`, `pageSize`, `totalCount`, `totalPages` — tidak berubah
- Tab dashboard dan settings — tidak tersentuh
- File lain selain App.tsx

## SCOPE LIMITS
- Hanya refactor visual structure pagination — behavior harus identik dengan sebelumnya
- Tidak mengubah behavior `!isFilterActive` (tetap ada pageSize selector + numbered pages)
- Tidak mengubah behavior `isFilterActive` (tetap hanya prev/next)

</boundaries>

<verification>
Sebelum declare plan complete:
- [ ] `npx tsc --noEmit` berjalan tanpa error
- [ ] Di App.tsx hanya ada SATU blok pagination di dalam `activeTab === 'data'` section
- [ ] Kondisi `!isFilterActive && totalCount > 0` dan `isFilterActive && totalCount > 0` TIDAK ada lagi sebagai dua blok terpisah
- [ ] `{totalCount > 0 && (...)}` menjadi wrapper tunggal
- [ ] Page size selector hanya tampil saat `!isFilterActive`
- [ ] Numbered pages hanya tampil saat `!isFilterActive` di breakpoint sm+
- [ ] Page indicator "X / Y" selalu tampil (visible di mobile; di desktop hanya saat filter aktif)
- [ ] Tidak ada perubahan fungsional — pagination behavior identik sebelum dan sesudah
</verification>

<success_criteria>
- Task 1 selesai
- TypeScript compile bersih
- AC-1 dan AC-2 terpenuhi
- App.tsx pagination section berkurang dari ~90 baris menjadi ~55 baris (estimasi)
- Tidak ada regresi pada filter, CRUD, auth
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/05-kasbon-ui-improvement/05-02-SUMMARY.md`
</output>
