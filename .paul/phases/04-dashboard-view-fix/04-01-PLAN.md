---
phase: 04-dashboard-view-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/Header.tsx, App.tsx]
autonomous: true
---

<objective>
## Goal
Sembunyikan semua action buttons (Ekspor, Tambah Data, Edit, Delete) di tab Dashboard sehingga halaman menjadi pure view-only.

## Purpose
Dashboard menampilkan ringkasan finansial dan "Transaksi Terakhir" — bukan tempat edit data. Action buttons yang muncul di sana misleading dan tidak sesuai konteks.

## Output
- `components/Header.tsx` — tombol Ekspor + Tambah Data hidden saat `activeTab === 'dashboard'`
- `App.tsx` — TransactionList di dashboard dirender dengan `userRole="user"` untuk suppress edit/delete
</objective>

<context>
@.paul/phases/04-dashboard-view-fix/CONTEXT.md
@components/Header.tsx
@App.tsx
</context>

<acceptance_criteria>

## AC-1: Header tidak tampilkan action buttons di dashboard
```gherkin
Given user berada di tab Dashboard
When halaman dirender
Then tombol Ekspor dan Tambah Data tidak terlihat di header
```

## AC-2: TransactionList di dashboard tidak tampilkan Edit/Delete
```gherkin
Given user adalah admin dan berada di tab Dashboard
When melihat section "Transaksi Terakhir"
Then tombol Edit dan Delete tidak muncul di setiap row
```

## AC-3: Tab Data tetap full action mode
```gherkin
Given user berpindah ke tab Data
When halaman dirender
Then tombol Ekspor, Tambah Data (untuk admin), Edit, dan Delete tetap tersedia
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Kondisional hide Ekspor + Tambah Data di Header</name>
  <files>components/Header.tsx</files>
  <action>
    Wrap tombol Ekspor dan Tambah Data dengan kondisi `activeTab !== 'dashboard'`.
    Tombol theme toggle (Moon/Sun) tetap tampil di semua tab — JANGAN ikut di-wrap.

    Sebelum (baris 48-58):
    ```tsx
    <button onClick={onOpenExport} className="...">
      <FileDown size={20} />
      <span className="hidden sm:inline ml-2 font-medium">Ekspor</span>
    </button>

    {isAdmin && (
      <button onClick={onOpenForm} className="...">
        <Plus size={20} />
        <span className="hidden sm:inline">Tambah Data</span>
      </button>
    )}
    ```

    Sesudah:
    ```tsx
    {activeTab !== 'dashboard' && (
      <>
        <button onClick={onOpenExport} className="...">
          <FileDown size={20} />
          <span className="hidden sm:inline ml-2 font-medium">Ekspor</span>
        </button>

        {isAdmin && (
          <button onClick={onOpenForm} className="...">
            <Plus size={20} />
            <span className="hidden sm:inline">Tambah Data</span>
          </button>
        )}
      </>
    )}
    ```

    Jaga semua className dan props tombol tetap identik — hanya tambahkan wrapper kondisional.
  </action>
  <verify>Inspeksi kode: kedua tombol ada di dalam `{activeTab !== 'dashboard' && (...)}`</verify>
  <done>AC-1 satisfied: Ekspor + Tambah Data tidak muncul saat activeTab === 'dashboard'</done>
</task>

<task type="auto">
  <name>Task 2: Pass userRole="user" ke TransactionList di dashboard</name>
  <files>App.tsx</files>
  <action>
    Di App.tsx, cari TransactionList yang dirender di dalam blok `activeTab === 'dashboard'`
    (sekitar baris 362). Ubah `userRole={user.role}` menjadi `userRole="user"`.

    UserRole type adalah `'admin' | 'user'` — `'user'` adalah nilai valid yang membuat
    `canEdit = false` di TransactionList, sehingga kolom/tombol Edit & Delete tidak dirender.

    Sebelum:
    ```tsx
    <TransactionList
      transactions={processedData.allTransactionsWithBalance.slice(0, 5)}
      onEdit={() => {}}
      onDelete={() => {}}
      userRole={user.role}
    />
    ```

    Sesudah:
    ```tsx
    <TransactionList
      transactions={processedData.allTransactionsWithBalance.slice(0, 5)}
      onEdit={() => {}}
      onDelete={() => {}}
      userRole="user"
    />
    ```

    JANGAN ubah TransactionList di blok `activeTab === 'data'` — itu harus tetap `userRole={user.role}`.
  </action>
  <verify>Inspeksi kode: `userRole="user"` di dashboard context, `userRole={user.role}` di data context</verify>
  <done>AC-2 satisfied: Edit/Delete tidak muncul di Transaksi Terakhir meski user adalah admin</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `components/TransactionList.tsx` — logika `canEdit = userRole === 'admin'` sudah benar, tidak perlu diubah
- `TransactionList` di blok `activeTab === 'data'` — harus tetap `userRole={user.role}`
- Semua logic bisnis CRUD dan export — tidak berubah

## SCOPE LIMITS
- Hanya conditional rendering — tidak ada prop baru, tidak ada abstraksi baru
- Tidak ada perubahan TypeScript types
- Tidak ada perubahan styling atau design system
</boundaries>

<verification>
Sebelum declare complete:
- [ ] `npx tsc --noEmit` — TypeScript clean, 0 error
- [ ] Header.tsx: tombol Ekspor + Tambah Data ada di dalam `{activeTab !== 'dashboard' && (...)}`
- [ ] App.tsx dashboard TransactionList: `userRole="user"` terpasang
- [ ] App.tsx data TransactionList: `userRole={user.role}` tidak berubah
</verification>

<success_criteria>
- AC-1, AC-2, AC-3 terpenuhi
- TypeScript 0 error
- Hanya 2 file yang berubah (Header.tsx, App.tsx)
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/04-dashboard-view-fix/04-01-SUMMARY.md`
</output>
