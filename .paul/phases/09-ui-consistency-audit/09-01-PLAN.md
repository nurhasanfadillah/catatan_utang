---
phase: 09-ui-consistency-audit
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - .paul/phases/09-ui-consistency-audit/AUDIT.md
autonomous: true
---

<objective>
## Goal
Audit konsistensi visual UI seluruh aplikasi dan hasilkan laporan temuan per komponen.

## Purpose
Design system sudah ditetapkan di Phase 03 (slate palette, indigo accent). Audit ini memastikan semua komponen dan tab konsisten — sebelum dilakukan fix di phase berikutnya.

## Output
- `AUDIT.md` di direktori phase ini — laporan temuan per file, dengan kolom: lokasi, masalah, seharusnya
</objective>

<context>
@.paul/phases/09-ui-consistency-audit/CONTEXT.md
@App.tsx
@components/Sidebar.tsx
@components/Header.tsx
@components/StatsCard.tsx
@components/TransactionList.tsx
@components/TransactionForm.tsx
@components/ConfirmationModal.tsx
@components/ExportModal.tsx
@components/LoginScreen.tsx
</context>

<acceptance_criteria>

## AC-1: Semua Area Teraudit
```gherkin
Given 8 komponen dan 3 tab di App.tsx
When audit selesai
Then AUDIT.md berisi temuan untuk setiap file yang diaudit
  Dan setiap temuan memiliki: lokasi spesifik, masalah, nilai yang seharusnya
  Dan komponen yang sudah konsisten dicatat sebagai "✓ OK"
```

## AC-2: 4 Dimensi Dicakup
```gherkin
Given design system reference (slate palette, indigo accent, spacing)
When laporan dibaca
Then setiap temuan diklasifikasi ke salah satu dari: Warna, Spacing, Typography, State Visual
  Dan ada ringkasan total temuan per dimensi di bagian atas laporan
```

## AC-3: Siap Jadi Input Phase Fix
```gherkin
Given AUDIT.md yang dihasilkan
When developer memulai phase fix berikutnya
Then laporan cukup spesifik untuk langsung dijadikan task list
  Dan setiap temuan bisa di-fix tanpa perlu buka source lagi
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Audit App.tsx — 3 tab sections (Dashboard, Data, Settings)</name>
  <files>.paul/phases/09-ui-consistency-audit/AUDIT.md</files>
  <action>
    Baca App.tsx secara menyeluruh. Fokus pada:
    - Section `activeTab === 'dashboard'`: card, spacing, layout
    - Section `activeTab === 'data'`: header, filter bar, pagination controls
    - Section `activeTab === 'settings'`: card layout, toggle style

    Untuk setiap inkonsistensi yang ditemukan, catat:
    - File: App.tsx
    - Lokasi: baris atau elemen spesifik
    - Dimensi: Warna / Spacing / Typography / State Visual
    - Masalah: nilai aktual yang dipakai
    - Seharusnya: nilai sesuai design system

    Design system reference:
    - bg: slate-950 (page), slate-900 (content), slate-800 (card)
    - border: slate-700
    - accent: indigo-500/600
    - text: slate-100 (primary), slate-400 (secondary)
    - danger: rose-500, success: emerald-500

    Mulai tulis AUDIT.md dengan:
    - Header laporan + design system reference ringkas
    - Bagian "App.tsx" dengan temuan Task 1
  </action>
  <verify>AUDIT.md ada dan berisi section "App.tsx" dengan temuan atau "✓ OK"</verify>
  <done>AC-1 partial: App.tsx teraudit dan tercatat di AUDIT.md</done>
</task>

<task type="auto">
  <name>Task 2: Audit shared components (Sidebar, Header, StatsCard, TransactionList)</name>
  <files>.paul/phases/09-ui-consistency-audit/AUDIT.md</files>
  <action>
    Baca masing-masing file:
    - components/Sidebar.tsx
    - components/Header.tsx
    - components/StatsCard.tsx
    - components/TransactionList.tsx

    Untuk setiap komponen, audit 4 dimensi:
    1. **Warna**: bg, border, text — sesuai design system?
    2. **Spacing**: padding/gap seragam antar komponen serupa?
    3. **Typography**: ukuran font, weight, label style konsisten?
    4. **State Visual**: hover, active, disabled state ada dan konsisten?

    Append ke AUDIT.md bagian per komponen.
  </action>
  <verify>AUDIT.md berisi section untuk Sidebar, Header, StatsCard, TransactionList</verify>
  <done>AC-1 partial: 4 shared components teraudit</done>
</task>

<task type="auto">
  <name>Task 3: Audit modals & forms (TransactionForm, ConfirmationModal, ExportModal, LoginScreen) + ringkasan</name>
  <files>.paul/phases/09-ui-consistency-audit/AUDIT.md</files>
  <action>
    Baca masing-masing file:
    - components/TransactionForm.tsx
    - components/ConfirmationModal.tsx
    - components/ExportModal.tsx
    - components/LoginScreen.tsx

    Audit 4 dimensi yang sama. Perhatian khusus:
    - Modal: backdrop, panel bg, border, close button style
    - Form: input styling (border, focus ring, placeholder color)
    - Button: primary vs danger vs secondary — konsistensi style

    Setelah semua temuan dicatat, tambahkan bagian **RINGKASAN** di atas laporan:
    ```
    ## Ringkasan

    Total temuan: N
    - Warna: N
    - Spacing: N
    - Typography: N
    - State Visual: N

    Komponen OK (tidak ada temuan): [list]
    Komponen dengan temuan: [list dengan jumlah temuan]
    ```
  </action>
  <verify>AUDIT.md berisi section untuk semua 4 modal/form dan ada bagian Ringkasan di atas</verify>
  <done>AC-1, AC-2, AC-3 satisfied: semua komponen teraudit, ringkasan ada, laporan siap jadi input phase fix</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Semua source file komponen — ini phase audit, bukan fix
- App.tsx — hanya dibaca, tidak diubah

## SCOPE LIMITS
- Tidak ada code changes di phase ini
- Output hanya AUDIT.md
- Tidak perlu jalankan dev server atau build

</boundaries>

<verification>
- [ ] AUDIT.md ada di `.paul/phases/09-ui-consistency-audit/AUDIT.md`
- [ ] Semua 9 file diaudit (App.tsx + 8 komponen)
- [ ] Setiap temuan punya: lokasi, dimensi, masalah, seharusnya
- [ ] Komponen tanpa masalah dicatat "✓ OK"
- [ ] Ringkasan total temuan per dimensi ada di atas laporan
</verification>

<success_criteria>
- Semua task selesai
- AUDIT.md lengkap dan actionable
- Laporan siap jadi input planning phase fix berikutnya
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/09-ui-consistency-audit/09-01-SUMMARY.md`
</output>
