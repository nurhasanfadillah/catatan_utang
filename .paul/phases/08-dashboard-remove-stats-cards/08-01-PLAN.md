---
phase: 08-dashboard-remove-stats-cards
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Hapus card "Total Tagihan" dan "Total Kasbon" dari dashboard, berikut state dan import yang tidak lagi diperlukan.
</objective>

<context>
@.paul/STATE.md
@App.tsx
</context>

<acceptance_criteria>

## AC-1: Card Tagihan & Kasbon Dihapus
```gherkin
Given user membuka tab Dashboard
When halaman render
Then hanya ada 1 card: "Saldo Saat Ini" (StatsCard balance)
  Dan tidak ada card Total Tagihan atau Total Kasbon
  Dan npx tsc --noEmit tidak ada error (no unused vars)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Hapus card 2-kolom, state income/expense, dan import TrendingUp/TrendingDown</name>
  <files>App.tsx</files>
  <action>
    Lakukan 4 perubahan di App.tsx:

    **1. Import:** Hapus `TrendingUp` dan `TrendingDown` dari import lucide-react
    (hanya dipakai di compact cards yang akan dihapus)

    **2. State:** Hapus 2 baris state di Balance State section:
    ```tsx
    const [globalTotalIncome, setGlobalTotalIncome] = useState(0);
    const [globalTotalExpense, setGlobalTotalExpense] = useState(0);
    ```

    **3. fetchData:** Hapus 2 setter di dalam fetchData:
    ```tsx
    setGlobalTotalIncome(summary.income);
    setGlobalTotalExpense(summary.expense);
    ```

    **4. Dashboard JSX:** Hapus seluruh block `<div className="grid grid-cols-2 gap-4">` beserta
    kedua card di dalamnya (Total Tagihan dan Total Kasbon).
    Sisakan hanya `<StatsCard title="Saldo Saat Ini" ... />`.
  </action>
  <verify>npx tsc --noEmit — tidak ada error</verify>
  <done>AC-1 satisfied: hanya balance card tersisa, TypeScript clean</done>
</task>

</tasks>

<output>
Setelah selesai, buat `.paul/phases/08-dashboard-remove-stats-cards/08-01-SUMMARY.md`
</output>
