---
phase: 03-ui-modernization
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - components/LoginScreen.tsx
  - components/Sidebar.tsx
  - components/Header.tsx
  - components/StatsCard.tsx
autonomous: false
---

<objective>
## Goal
Redesign LoginScreen, Sidebar, Header, dan StatsCard ke dark fintech palette: slate-900 background, indigo-500 accent, gradient CTA.

## Purpose
03-01 sudah setup PostCSS pipeline. Tailwind v3 sudah include semua default colors (slate, indigo, violet, purple, emerald, rose) — tidak perlu install apapun lagi. 03-02 menggunakan colors tersebut untuk visual redesign: mengganti gray-* dengan slate-*, brand-* dengan indigo/gradient, menghasilkan tampilan modern fintech yang konsisten.

## Output
- LoginScreen: dark gradient bg, glassmorphism card, gradient badge+button
- Sidebar: slate-900 bg (dark), indigo active state dengan left accent border
- Header: slate-900/95 backdrop-blur, gradient "Tambah Data" button
- StatsCard: balance = full gradient hero (text-4xl), income/expense = accent border cards
</objective>

<context>
## Project Context
@.paul/STATE.md
@.paul/phases/03-ui-modernization/DISCOVERY.md
@.paul/phases/03-ui-modernization/03-01-SUMMARY.md

## Source Files
@components/LoginScreen.tsx
@components/Sidebar.tsx
@components/Header.tsx
@components/StatsCard.tsx
</context>

<acceptance_criteria>

## AC-1: LoginScreen dark fintech aesthetic
```gherkin
Given app dibuka di http://localhost:3000
When halaman login tampil (sebelum login)
Then background adalah gradient gelap from-slate-900 via-indigo-950 to-slate-900
And card adalah glassmorphism bg-slate-800/80 backdrop-blur-xl
And logo badge adalah gradient from-indigo-500 to-violet-600
And submit button adalah gradient from-indigo-500 to-violet-600
And semua text dalam card menggunakan slate/white (bukan gray)
```

## AC-2: Sidebar dan Header menggunakan slate palette + indigo active state
```gherkin
Given user sudah login dan dashboard tampil
When dark mode aktif (default)
Then sidebar background adalah bg-slate-900
And header background adalah bg-slate-900/95 dengan backdrop-blur-sm
And active nav item memiliki bg-indigo-500/10 text-indigo-400 dan border-l-2 border-indigo-500
And "Tambah Data" button adalah gradient from-indigo-500 to-violet-600
```

## AC-3: StatsCard balance adalah gradient hero card, income/expense adalah accent border
```gherkin
Given dashboard tampil dengan StatsCard
When variant="balance" dirender
Then card background adalah gradient from-indigo-500 via-purple-600 to-violet-700
And nilai saldo menggunakan text-4xl text-white
And icon background adalah bg-white/20 dengan icon text-white
When variant="income" atau variant="expense" dirender
Then card memiliki border-l-4 dengan warna emerald-500 (income) atau rose-500 (expense)
And background adalah bg-white dark:bg-slate-800
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Redesign LoginScreen.tsx ke dark fintech aesthetic</name>
  <files>components/LoginScreen.tsx</files>
  <action>
    Replace Tailwind classes only. Semua logic, state, props, handlers TIDAK BERUBAH.

    Perubahan class satu per satu:

    1. Outer wrapper div:
       FROM: `min-h-screen bg-gray-50 dark:bg-gray-900 flex items-center justify-center p-4`
       TO:   `min-h-screen bg-gradient-to-br from-slate-900 via-indigo-950 to-slate-900 flex items-center justify-center p-4`

    2. Card div:
       FROM: `max-w-md w-full bg-white dark:bg-gray-800 rounded-2xl shadow-xl border border-gray-100 dark:border-gray-700 overflow-hidden`
       TO:   `max-w-md w-full bg-slate-800/80 backdrop-blur-xl rounded-2xl shadow-2xl border border-slate-700/50 overflow-hidden`

    3. Logo badge div:
       FROM: `w-16 h-16 bg-brand-600 rounded-2xl flex items-center justify-center text-white font-bold text-3xl shadow-lg shadow-brand-500/30 mx-auto mb-4`
       TO:   `w-16 h-16 bg-gradient-to-br from-indigo-500 to-violet-600 rounded-2xl flex items-center justify-center text-white font-bold text-3xl shadow-lg shadow-indigo-500/40 mx-auto mb-4`

    4. Title h1:
       FROM: `text-2xl font-bold text-gray-900 dark:text-white`
       TO:   `text-2xl font-bold text-white`

    5. Subtitle p:
       FROM: `text-gray-500 dark:text-gray-400 mt-2`
       TO:   `text-slate-400 mt-2`

    6. Error div:
       FROM: `p-3 rounded-lg bg-rose-50 border border-rose-100 text-rose-600 text-sm text-center font-medium`
       TO:   `p-3 rounded-lg bg-rose-500/10 border border-rose-500/20 text-rose-400 text-sm text-center font-medium`

    7. Form labels (kedua label — username dan password):
       FROM: `block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1.5`
       TO:   `block text-sm font-medium text-slate-300 mb-1.5`

    8. Input icon divs (absolute inset-y-0):
       FROM: `text-gray-400` (pada User icon dan Lock icon)
       TO:   `text-slate-400`

    9. Input elements (kedua input — username dan password):
       FROM: `block w-full pl-10 pr-3 py-3 border border-gray-300 dark:border-gray-600 rounded-xl leading-5 bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-brand-500 sm:text-sm transition-all`
       TO:   `block w-full pl-10 pr-3 py-3 border border-slate-600 rounded-xl leading-5 bg-slate-700 text-white placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm transition-all`

    10. Submit button:
        FROM: `w-full flex justify-center items-center gap-2 py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-brand-600 hover:bg-brand-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-brand-500 disabled:opacity-50 disabled:cursor-not-allowed transition-all`
        TO:   `w-full flex justify-center items-center gap-2 py-3 px-4 border border-transparent rounded-xl shadow-sm text-sm font-medium text-white bg-gradient-to-r from-indigo-500 to-violet-600 hover:from-indigo-600 hover:to-violet-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 shadow-indigo-500/25 disabled:opacity-50 disabled:cursor-not-allowed transition-all`

    11. Dev mode button (Akses Masuk Paksa):
        FROM: `w-full flex justify-center items-center gap-2 py-3 px-4 border border-gray-300 dark:border-gray-600 rounded-xl shadow-sm text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition-all`
        TO:   `w-full flex justify-center items-center gap-2 py-3 px-4 border border-slate-600 rounded-xl shadow-sm text-sm font-medium text-slate-400 bg-slate-700/50 hover:bg-slate-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-500 transition-all`

    12. Footer div:
        FROM: `bg-gray-50 dark:bg-gray-700/30 px-8 py-4 border-t border-gray-100 dark:border-gray-700 text-center`
        TO:   `bg-slate-800/50 px-8 py-4 border-t border-slate-700/50 text-center`

    13. Footer p:
        FROM: `text-xs text-gray-500 dark:text-gray-400`
        TO:   `text-xs text-slate-500`
  </action>
  <verify>
    Grep hasil edit:
    - `grep -n "bg-gray\|bg-white\|text-gray\|border-gray\|brand-" components/LoginScreen.tsx` → harus KOSONG
    - `grep -n "from-slate-900\|bg-slate-800/80\|from-indigo-500" components/LoginScreen.tsx` → harus ada match
    - `npx tsc --noEmit 2>&1 | grep -v "migrate-from-supabase"` → kosong (no errors)
  </verify>
  <done>AC-1 prerequisite: LoginScreen menggunakan dark fintech palette dengan gradient background dan glassmorphism card.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Sidebar.tsx + Header.tsx ke slate/indigo palette</name>
  <files>components/Sidebar.tsx, components/Header.tsx</files>
  <action>
    Replace Tailwind classes only. Semua logic, props, state, handlers TIDAK BERUBAH.

    ═══ SIDEBAR.TSX ═══

    1. `aside` element — bg + border classes:
       FROM: `bg-white dark:bg-gray-800 border-r border-gray-200 dark:border-gray-700`
       TO:   `bg-white dark:bg-slate-900 border-r border-gray-200 dark:border-slate-800`

    2. Sidebar header div (flex items-center ... h-20 border-b):
       FROM: `border-gray-100 dark:border-gray-700`
       TO:   `border-gray-100 dark:border-slate-800`

    3. Logo badge div (w-10 h-10):
       FROM: `bg-brand-600 ... shadow-brand-500/30`
       TO:   `bg-gradient-to-br from-indigo-500 to-violet-600 shadow-indigo-500/30`

    4. Subtitle "Financial Manager" p:
       FROM: `text-xs text-gray-500 dark:text-gray-400`
       TO:   `text-xs text-slate-400`

    5. Desktop collapse button:
       FROM: `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 text-gray-500 dark:text-gray-400 ... hover:text-brand-600 dark:hover:text-brand-400`
       TO:   `bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 text-slate-500 dark:text-slate-400 ... hover:text-indigo-600 dark:hover:text-indigo-400`

    6. Nav item — active state class:
       FROM: `bg-brand-50 text-brand-700 dark:bg-brand-900/20 dark:text-brand-300 font-semibold shadow-sm`
       TO:   `bg-indigo-500/10 text-indigo-600 dark:text-indigo-400 border-l-2 border-indigo-500 font-semibold`

    7. Nav item — inactive state class:
       FROM: `text-gray-600 dark:text-gray-400 hover:bg-gray-50 dark:hover:bg-gray-700/50`
       TO:   `text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800`

    8. Bottom section (p-4 border-t ... bg):
       FROM: `border-gray-100 dark:border-gray-700 bg-white dark:bg-gray-800`
       TO:   `border-gray-100 dark:border-slate-800 bg-white dark:bg-slate-900`

    9. User card inner div (bg-gray-50 rounded-2xl):
       FROM: `bg-gray-50 dark:bg-gray-700/50 ... border-gray-100 dark:border-gray-700`
       TO:   `bg-gray-50 dark:bg-slate-800/50 ... border-gray-100 dark:border-slate-700`

    10. Admin user avatar:
        FROM: `from-brand-500 to-indigo-600`
        TO:   `from-indigo-500 to-violet-600`

    11. User role text:
        FROM: `text-xs text-gray-500 dark:text-gray-400 truncate capitalize`
        TO:   `text-xs text-slate-500 dark:text-slate-400 truncate capitalize`

    ═══ HEADER.TSX ═══

    1. `header` element:
       FROM: `h-20 flex items-center justify-between px-6 md:px-8 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 z-10 sticky top-0`
       TO:   `h-20 flex items-center justify-between px-6 md:px-8 bg-white dark:bg-slate-900/95 backdrop-blur-sm border-b border-gray-200 dark:border-slate-800 z-10 sticky top-0`

    2. Menu (hamburger) button:
       FROM: `md:hidden p-2 rounded-lg text-gray-500 hover:bg-gray-100 dark:hover:bg-gray-700`
       TO:   `md:hidden p-2 rounded-lg text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800`

    3. Subtitle p:
       FROM: `text-xs md:text-sm text-gray-500 dark:text-gray-400 hidden sm:block`
       TO:   `text-xs md:text-sm text-slate-500 dark:text-slate-400 hidden sm:block`

    4. Theme toggle button:
       FROM: `p-2.5 rounded-xl text-gray-500 bg-gray-50 hover:bg-gray-100 dark:bg-gray-700/50 dark:hover:bg-gray-700 transition-all border border-gray-200 dark:border-gray-600 hidden sm:flex`
       TO:   `p-2.5 rounded-xl text-slate-500 bg-slate-50 hover:bg-slate-100 dark:bg-slate-800 dark:hover:bg-slate-700 transition-all border border-slate-200 dark:border-slate-700 hidden sm:flex`

    5. Export button:
       FROM: `flex items-center justify-center p-2.5 sm:px-4 sm:py-2.5 rounded-xl border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-all`
       TO:   `flex items-center justify-center p-2.5 sm:px-4 sm:py-2.5 rounded-xl border border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 transition-all`

    6. "Tambah Data" button:
       FROM: `flex items-center gap-2 bg-brand-600 hover:bg-brand-700 text-white px-4 py-2.5 sm:px-5 rounded-xl text-sm font-medium shadow-lg shadow-brand-500/20 transition-all hover:scale-105 active:scale-95`
       TO:   `flex items-center gap-2 bg-gradient-to-r from-indigo-500 to-violet-600 hover:from-indigo-600 hover:to-violet-700 text-white px-4 py-2.5 sm:px-5 rounded-xl text-sm font-medium shadow-lg shadow-indigo-500/20 transition-all hover:scale-105 active:scale-95`
  </action>
  <verify>
    - `grep -n "bg-gray-8\|brand-6\|brand-7\|brand-50\|brand-300\|brand-400\|bg-gray-700" components/Sidebar.tsx` → kosong
    - `grep -n "slate-900\|indigo-500\|indigo-400" components/Sidebar.tsx` → ada match
    - `grep -n "bg-gray-8\|brand-6\|brand-7" components/Header.tsx` → kosong
    - `grep -n "slate-900/95\|from-indigo-500" components/Header.tsx` → ada match
    - `npx tsc --noEmit 2>&1 | grep -v "migrate-from-supabase"` → kosong
  </verify>
  <done>AC-2 satisfied: Sidebar menggunakan slate-900 (dark) dan indigo active state, Header menggunakan slate-900/95 backdrop-blur.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign StatsCard.tsx — balance hero gradient, income/expense accent border</name>
  <files>components/StatsCard.tsx</files>
  <action>
    Rewrite file sepenuhnya. Props interface dan type TIDAK BERUBAH — hanya variantConfig dan render JSX yang diupdate.

    Tulis ulang components/StatsCard.tsx dengan konten berikut:

    ```tsx
    import React from 'react';
    import { LucideIcon } from 'lucide-react';
    import { formatCurrency } from '../utils/formatters';

    type StatsCardVariant = 'balance' | 'income' | 'expense';

    interface StatsCardProps {
      title: string;
      value: number;
      icon: LucideIcon;
      variant: StatsCardVariant;
    }

    const variantConfig: Record<StatsCardVariant, {
      wrapper: string;
      titleColor: string;
      valueColor: string;
      valueSize: string;
      iconBg: string;
      iconColor: string;
    }> = {
      balance: {
        wrapper: 'bg-gradient-to-br from-indigo-500 via-purple-600 to-violet-700 shadow-lg shadow-indigo-500/25',
        titleColor: 'text-indigo-100',
        valueColor: 'text-white',
        valueSize: 'text-4xl',
        iconBg: 'bg-white/20',
        iconColor: 'text-white',
      },
      income: {
        wrapper: 'bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 border-l-4 border-l-emerald-500',
        titleColor: 'text-slate-500 dark:text-slate-400',
        valueColor: 'text-slate-900 dark:text-white',
        valueSize: 'text-2xl',
        iconBg: 'bg-emerald-500/10',
        iconColor: 'text-emerald-500',
      },
      expense: {
        wrapper: 'bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 border-l-4 border-l-rose-500',
        titleColor: 'text-slate-500 dark:text-slate-400',
        valueColor: 'text-slate-900 dark:text-white',
        valueSize: 'text-2xl',
        iconBg: 'bg-rose-500/10',
        iconColor: 'text-rose-500',
      },
    };

    export const StatsCard: React.FC<StatsCardProps> = ({ title, value, icon: Icon, variant }) => {
      const { wrapper, titleColor, valueColor, valueSize, iconBg, iconColor } = variantConfig[variant];
      return (
        <div className={`rounded-xl p-6 ${wrapper}`}>
          <div className="flex items-center justify-between">
            <div>
              <p className={`text-sm font-medium ${titleColor}`}>{title}</p>
              <h3 className={`font-bold mt-2 ${valueColor} ${valueSize}`}>
                {formatCurrency(value)}
              </h3>
            </div>
            <div className={`p-3 rounded-lg ${iconBg}`}>
              <Icon className={`w-6 h-6 ${iconColor}`} />
            </div>
          </div>
        </div>
      );
    };
    ```

    Catatan:
    - `via-purple-600` available di Tailwind v3 defaults — tidak perlu config tambahan
    - `border-l-4 border-l-emerald-500` override left border: `border-l-color` lebih spesifik dari `border-color` shorthand, sehingga left border selalu emerald/rose regardless dark mode
    - `bg-white/20` untuk icon bg di balance variant menggunakan opacity modifier Tailwind v3 syntax
  </action>
  <verify>
    - `grep -n "from-indigo-500 via-purple-600" components/StatsCard.tsx` → ada match
    - `grep -n "text-4xl" components/StatsCard.tsx` → ada (di variantConfig balance.valueSize)
    - `grep -n "border-l-4 border-l-emerald-500" components/StatsCard.tsx` → ada (income)
    - `grep -n "border-l-4 border-l-rose-500" components/StatsCard.tsx` → ada (expense)
    - Tidak ada `bg-brand-` atau `container` atau `.replace(` di file
    - `npx tsc --noEmit 2>&1 | grep -v "migrate-from-supabase"` → kosong
  </verify>
  <done>AC-3 satisfied: StatsCard balance adalah gradient hero card dengan text-4xl, income/expense menggunakan accent border pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    4 komponen diredesain ke dark fintech palette:
    - LoginScreen: dark gradient bg (slate-900→indigo-950→slate-900), glassmorphism card (bg-slate-800/80 backdrop-blur), gradient logo+button
    - Sidebar: slate-900 bg (dark), indigo active state (bg-indigo-500/10 + border-l-2), gradient logo
    - Header: slate-900/95 backdrop-blur, gradient "Tambah Data" button
    - StatsCard balance: gradient hero (indigo→purple→violet), text-4xl; income/expense: accent border
  </what-built>
  <how-to-verify>
    1. Jalankan: `npm run dev`
    2. Buka http://localhost:3000

    CEK LOGIN (sebelum login):
    a. Background: gelap gradient biru-ungu (bukan abu-abu biasa)
    b. Card: transparan gelap dengan blur effect (glassmorphism)
    c. Logo badge "K": gradient ungu-biru (bukan solid biru)
    d. Submit button "Masuk Sistem": gradient ungu-biru
    e. Form inputs: background gelap (slate-700)

    3. Klik "Akses Masuk Paksa" untuk login tanpa credentials

    CEK DASHBOARD (setelah login):
    f. Sidebar dark: background gelap (slate-900), bukan gray-800
    g. Active nav item: ada garis tipis biru di kiri + background biru transparan
    h. "Saldo Saat Ini" StatsCard: card penuh gradient ungu-biru, angka BESAR (text-4xl), icon putih di bg putih transparan
    i. Header: background gelap transparan dengan blur effect (glassmorphism)
    j. "Tambah Data" button di header: gradient ungu-biru

    CEK DARK/LIGHT MODE:
    k. Toggle dark/light — tidak ada visual break, UI tampil benar di kedua mode

    CEK MOBILE (resize browser ke <768px):
    l. Sidebar slide-in masih bekerja normal

    5. Jalankan: `npm run build` — harus exit 0 tanpa error
  </how-to-verify>
  <resume-signal>Ketik "approved" jika visual OK, atau deskripsikan masalah yang ditemukan</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `api/` directory (Vercel API routes — bukan scope UI)
- `services/api.ts`, `services/auth.ts` (backend logic)
- `db/`, `drizzle.config.ts` (database config)
- `tailwind.config.ts` (Tailwind v3 defaults sudah include semua colors yang diperlukan — tidak perlu perubahan)
- `App.tsx` (layout structure tetap — redesign hanya di komponen level)
- `components/TransactionList.tsx`, `components/TransactionForm.tsx`, `components/ExportModal.tsx`, `components/ConfirmationModal.tsx` (scope 03-03)
- Semua business logic, props interface, state, event handlers di komponen yang dimodifikasi
- Teks/content (label, placeholder, error messages, copyright) — hanya visual yang berubah

## SCOPE LIMITS
- Hanya perubahan Tailwind class strings — tidak ada perubahan JSX structure, props API, atau logic
- Tidak install library baru atau tambah dependencies
- Tidak ubah tailwind.config.ts
- Tidak touch App.tsx meskipun App.tsx masih menggunakan brand-* atau gray-* — itu akan terselesaikan di 03-03 atau dibiarkan jika tidak visible
- brand-* classes di file lain (TransactionList, dll) dibiarkan — tetap berfungsi karena brand-* masih di tailwind.config.ts

</boundaries>

<verification>
Sebelum declare plan complete:
- [ ] `grep -n "bg-gray\|bg-white\|text-gray\|border-gray\|brand-" components/LoginScreen.tsx` kosong
- [ ] `grep -n "brand-6\|brand-7\|bg-gray-8" components/Sidebar.tsx` kosong
- [ ] `grep -n "brand-6\|brand-7\|bg-gray-8" components/Header.tsx` kosong
- [ ] `grep -n "from-indigo-500 via-purple-600" components/StatsCard.tsx` ada match
- [ ] `grep -n "text-4xl" components/StatsCard.tsx` ada match
- [ ] `npx tsc --noEmit 2>&1 | grep -v "migrate-from-supabase"` kosong
- [ ] `npm run build` exit code 0
- [ ] Checkpoint human-verify: "approved"
</verification>

<success_criteria>
- Semua 3 tasks selesai dengan qualify PASS
- Checkpoint human-verify approved
- Visual konsisten dengan spec di DISCOVERY.md
- Tidak ada regresi fungsional (login/logout, navigasi tab, dark mode toggle, sidebar mobile)
- Production build berhasil
</success_criteria>

<output>
Setelah completion, buat `.paul/phases/03-ui-modernization/03-02-SUMMARY.md`
</output>
