---
phase: 07-code-cleanup
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Hapus dead code: brand-* tokens dari tailwind.config.ts dan script migrate-from-supabase.ts yang sudah obsolete.
</objective>

<context>
@.paul/STATE.md
@tailwind.config.ts
@scripts/migrate-from-supabase.ts
</context>

<acceptance_criteria>

## AC-1: Tidak Ada Dead Code dari Supabase/brand
```gherkin
Given codebase setelah Phase 01-06
When developer membuka project
Then scripts/migrate-from-supabase.ts tidak ada (migration sudah selesai di Phase 02)
  Dan tailwind.config.ts tidak punya brand-* color tokens (tidak dipakai di mana pun)
  Dan npx tsc --noEmit tidak ada error (termasuk error migrate-from-supabase.ts)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Hapus migrate-from-supabase.ts dan brand-* tokens dari tailwind</name>
  <files>scripts/migrate-from-supabase.ts, tailwind.config.ts</files>
  <action>
    1. **Hapus file**: `scripts/migrate-from-supabase.ts`
       - Migration Supabase → NeonDB sudah selesai di Phase 02
       - File ini menyebabkan TS error karena @supabase/supabase-js tidak lagi diinstall
       - Tidak perlu archive — tidak berguna lagi

    2. **Edit `tailwind.config.ts`**: Hapus block `colors: { brand: { ... } }` dari theme.extend
       - brand-* tokens tidak dipakai di seluruh codebase (grep: zero hits)
       - Tinggalkan `fontFamily` tetap ada
  </action>
  <verify>npx tsc --noEmit — harus bersih (no errors)</verify>
  <done>AC-1 satisfied: file dihapus, brand-* hilang, TypeScript clean</done>
</task>

</tasks>

<output>
Setelah selesai, buat `.paul/phases/07-code-cleanup/07-01-SUMMARY.md`
</output>
